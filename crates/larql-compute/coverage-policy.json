{
 "policy_note": "Default policy is 90% line coverage per source file. After the larql-compute-metal extraction (ADR-019), this crate is CPU-side only \u2014 the trait, the CpuBackend impl, pipeline types, and env-option helpers. Every file is expected to clear the 90% floor without `exclude_globs` hacks. GPU-shader coverage lives in `crates/larql-compute-metal/coverage-policy.json`. 2026-06-06: the two inherited debt baselines (`attention/decode.rs` 55%, `kv_dispatch/cpu.rs` 88%) were retired \u2014 `attention/decode.rs` is now 92.3% and `kv_dispatch/cpu.rs` 97.1%. The q4k-direct decode path is driven directly via the `Q4kFixtureIndex` fixture (`run_attention_block_decode_step_q4k_direct`, no env var needed) plus an env-gated `attention_step` test (LARQL_Q4K_DIRECT_ATTN, OnceLock-owned by that single test). Residual misses in decode.rs are attention-bias / QK-norm / v-norm branches absent from the gemma3/tinymodel Q4K fixtures (no fixture carries attn biases); residual misses in cpu.rs are the indirect-matvec `coarse_decode_step{,_with_state}` arms, which need an index that supports cached-decode but NOT direct-matvec (structurally hard \u2014 both consume the same Q4_K slices). 2026-07-22 (DEC prep): the large-file splits (pipeline/, kv_dispatch/, attention/decode/, cpu/ops/{moe/expert,q4_common,q4k_q8k_dot}/) re-home those documented residual misses into dedicated files, where they no longer average against well-covered siblings: attention/decode/dispatch.rs carries the attention-bias/v-norm/multiplier branches (no fixture arch carries attn biases), kv_dispatch/cpu/dispatch.rs the structurally-hard indirect-matvec coarse arms, attention/decode/q4k_direct.rs scattered failure branches whose tests live in larql-kv. Baselines below are local --package lower bounds (CI's cross-crate measurement reads higher); raise toward 90 and retire when a bias-carrying fixture arch exists. 2026-07-23: CI moved to rust 1.97 whose coverage instrumentation shifts line attribution; the x86 CI profile (no aarch64-gated tests) measured three files under 90 after the DEC-prep merge: cached.rs 89.88 (the 1.97 clippy question-mark rewrite deleted covered lines), ffn/weight.rs 86.21 (FormatRoute dispatch shrank the file), tensors.rs 61.46 (untouched on this branch - pre-existing x86-profile gap newly surfaced). Baselines set just under measured; raise toward 90.",
 "include_globs": [
  "crates/larql-compute/src/*.rs",
  "crates/larql-compute/src/**/*.rs"
 ],
 "exclude_globs": [],
 "default_line_min_percent": 90.0,
 "total_line_min_percent": 95.0,
 "per_file_line_min_percent": {
  "crates/larql-compute/src/connectors/projector.rs": 80.0,
  "crates/larql-compute/src/attention/decode/dispatch.rs": 85.0,
  "crates/larql-compute/src/attention/decode/q4k_direct.rs": 87.0,
  "crates/larql-compute/src/kv_dispatch/cpu/dispatch.rs": 86.0,
  "crates/larql-compute/src/kquant_forward/cached.rs": 89.0,
  "crates/larql-compute/src/ffn/weight.rs": 85.0,
  "crates/larql-compute/src/kquant_forward/tensors.rs": 60.0
 }
}